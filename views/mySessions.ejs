<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="./css/style.css" />
    <title>Sessions</title>
    <script>
      function deleteSport(id) {
        fetch(`/sport/${id}`, {
          method: "delete",
        })
          .then((res) => {
            if (res.ok) {
              window.location.href = "/";
            }
          })
          .catch((err) => console.error(err));
      }
    </script>
  </head>
  <body>
    <div class="grid grid-cols-6 pt-10">
      <div class="col-start-3 col-span-2">
        <span class="mb-3 text-4xl font-semibold text-gray-600">
          <%= title %> </span
        ><span class="ml-5"
          ><a
            class="bg-blue-600 hover:bg-blue-700 text-white px-3 py-1.5 rounded font-medium mr-2 mb"
            href="/sport"
            >Home</a
          ></span
        >
        <hr class="my-5 h-0.5 bg-black opacity-50" />
        <div>
          <h1 class="mb-3 text-2xl font-bold text-gray-600">
            Upcoming sessions
          </h1>
          <table>
            <%=upComingSessions.length %> sessions Available<% for( var i = 0 ;
            i < upComingSessions.length ; i++) { %>
            <tr>
              <td>
                <a
                  href="/sessions/<%=upComingSessions[i].id %>"
                  class="text-green-600"
                >
                  <div
                    class="bg-white hover:bg-stone-50 my-0.5 border font-semibold border-gray-600 text-green-600 rounded p-2 w-full"
                  >
                    <div class="mr-18 display: inline">
                      <%=upComingSessions[i].playDate.toLocaleDateString('en-US',
                      { year: '2-digit', month: '2-digit', day: '2-digit', hour:
                      'numeric', minute: 'numeric', hour12: true })%>
                    </div>
                    <% if (upComingSessions[i].playersneeded>1) { %>
                    <div class="pl-5 pr-2 display: inline">
                      <%=upComingSessions[i].playersneeded %> slots
                    </div>
                    <% } %> <% if (upComingSessions[i].playersneeded===1) { %>
                    <div class="text-red-600 pl-5 pr-2 display: inline">
                      Last slot
                    </div>
                    <% } %> <% if (upComingSessions[i].playersneeded ===0) { %>
                    <div
                      class="text-green-1000 font-bold pl-5 pr-2 display: inline"
                    >
                      Fully Booked
                    </div>
                    <% } %>
                  </div>
                </a>
              </td>
            </tr>
            <% } %>
          </table>
        </div>
        <hr class="my-5 h-0.5 bg-black opacity-50" />
        <div>
          <h1 class="mb-3 text-2xl font-bold text-gray-600">
            Previous sessions
          </h1>
          <table>
            <%=previousSessions.length %> sessions Available<% for( var i = 0 ;
            i < previousSessions.length ; i++) { %>
            <tr>
              <td>
                <a
                  href="/sessions/<%=previousSessions[i].id %>"
                  class="text-green-600"
                >
                  <div
                    class="bg-white hover:bg-stone-50 my-0.5 border border-gray-600 font-semibold text-green-600 rounded p-2 w-full"
                  >
                    <div class="mr-18 display: inline">
                      <%=previousSessions[i].playDate.toLocaleDateString('en-US',
                      { year: '2-digit', month: '2-digit', day: '2-digit', hour:
                      'numeric', minute: 'numeric', hour12: true })%>
                    </div>
                    <% if (previousSessions[i].playersneeded>1) { %>
                    <div class="pl-5 pr-2 display: inline">
                      <%=previousSessions[i].playersneeded %> slots
                    </div>
                    <% } %> <% if (previousSessions[i].playersneeded===1) { %>
                    <div class="text-red-600 pl-5 pr-2 display: inline">
                      Last slot
                    </div>
                    <% } %> <% if (previousSessions[i].playersneeded ===0) { %>
                    <div
                      class="text-green-1000 font-bold pl-5 pr-2 display: inline"
                    >
                      Fully Booked
                    </div>
                    <% } %>
                  </div>
                </a>
              </td>
            </tr>
            <% } %>
          </table>
        </div>
        <hr class="my-5 h-0.5 bg-black opacity-50" />
        <div>
          <h5 class="text-2xl font-bold text-gray-600 pb-5">
            Sessions created by you
          </h5>
          <table>
            <%=createdUpcomingSessions.length %> sessions Available<% for( var i
            = 0 ; i < createdUpcomingSessions.length ; i++) { %>
            <tr>
              <td>
                <a
                  href="/sessions/<%=createdUpcomingSessions[i].id %>"
                  class="text-green-600"
                >
                  <div
                    class="bg-white hover:bg-stone-50 border border-gray-600 text-green-600 font-semibold rounded p-2 my-0.5 w-full"
                  >
                    <div class="mr-18 display: inline">
                      <%=createdUpcomingSessions[i].playDate.toLocaleDateString('en-US',
                      { year: '2-digit', month: '2-digit', day: '2-digit', hour:
                      'numeric', minute: 'numeric', hour12: true })%>
                    </div>
                    <% if (createdUpcomingSessions[i].playersneeded>1) { %>
                    <div class="pl-5 pr-2 display: inline">
                      <%=createdUpcomingSessions[i].playersneeded %> slots
                    </div>
                    <% } %> <% if (createdUpcomingSessions[i].playersneeded===1)
                    { %>
                    <div class="text-red-600 pl-5 pr-2 display: inline">
                      Last slot
                    </div>
                    <% } %> <% if (createdUpcomingSessions[i].playersneeded
                    ===0) { %>
                    <div
                      class="text-green-1000 font-bold pl-5 pr-2 display: inline"
                    >
                      Fully Booked
                    </div>
                    <% } %> <% if (createdUpcomingSessions[i].isCanceled ===true
                    ) { %>
                    <div
                      class="text-red-600 font-bold pl-5 pr-2 display: inline"
                    >
                      Canceled
                    </div>
                    <% } %>
                  </div>
                </a>
              </td>
            </tr>
            <% } %>
          </table>
        </div>
        <hr class="my-5 h-0.5 bg-black opacity-50" />
        <div>
          <h1 class="my-3 text-2xl font-bold text-gray-600">
            Canceled sessions
          </h1>
          <table>
            <%=canceledSessions.length %> sessions Available<% for( var i = 0 ;
            i < canceledSessions.length ; i++) { %>
            <tr>
              <td class="my-2">
                <a
                  href="/sessions/<%=canceledSessions[i].id %>"
                  class="text-green-600"
                >
                  <div
                    class="bg-white hover:bg-stone-50 my-0.5 border border-gray-600 font-semibold text-green-600 rounded p-2 w-full"
                  >
                    <div class="mr-18 display: inline">
                      <%=canceledSessions[i].playDate.toLocaleDateString('en-US',
                      { year: '2-digit', month: '2-digit', day: '2-digit', hour:
                      'numeric', minute: 'numeric', hour12: true })%>
                    </div>
                    <% if (canceledSessions[i].playersneeded>1) { %>
                    <div class="pl-5 pr-2 display: inline">
                      <%=canceledSessions[i].playersneeded %> slots
                    </div>
                    <% } %> <% if (canceledSessions[i].playersneeded===1) { %>
                    <div class="text-red-600 pl-5 pr-2 display: inline">
                      Last slot
                    </div>
                    <% } %> <% if (canceledSessions[i].playersneeded ===0) { %>
                    <div
                      class="text-green-1000 font-bold pl-5 pr-2 display: inline"
                    >
                      Fully Booked
                    </div>
                    <% } %><% if (canceledSessions[i].isCanceled ===true ) { %>
                    <div
                      class="text-red-600 font-bold pl-5 pr-2 display: inline"
                    >
                      Canceled
                    </div>
                    <% } %>
                  </div>
                </a>
              </td>
            </tr>
            <% } %>
          </table>
        </div>
        <div>
          <br />
          <a
            class="bg-red-600 hover:bg-red-800 text-white px-3 py-1 rounded font-semibold font-medium mr-2 mb mt-5"
            href="/signout"
            >Signout</a
          >
        </div>
      </div>
    </div>
  </body>
</html>
