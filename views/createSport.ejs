<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="./css/style.css" />
    <title>Sports Scheduler</title>
  </head>
  <body>
    <div class="grid grid-cols-6">
      <div class="col-start-3 col-span-2">
        <h2 class="text-xl font-semibold text-gray-600">Create new sport</h2>
        <form action="/sports" method="post">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <div class="py-3">
            <label for="title">Enter the name of Sport you want to create</label>
            <input
              name="title"
              class="border border-gray-600 text-gray-900 rounded p-2 w-full"
              type="text"
              placeholder="Enter Sport Name"
            />
          </div>
          <div class="py-3">
            <button
              type="submit"
              class="text-white bg-blue-600 hover:bg-blue-800 font-medium rounded px-5 py-2"
            >
              Submit
            </button>
          </div>
        </form>
        <div class="text-red-500"><% if(messages && messages.error && messages.error.length > 0)  { %>
          <% for(var i=0; i<messages.error.length; i++) { %>
                  <li > <%= messages.error[i] %></li>
          <% } %>
        <% } %></div>
      </div>
    </div>
  </body>
</html>
